{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.2.4"
  },
  "scripts": {
    "build": "lerna run build",
    "lerna:publish": "lerna publish --no-private"
  }
}
