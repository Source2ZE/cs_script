{
  "name": "cs-script-workspace",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.2.4",
    "prettier": "^3.6.2"
  },
  "scripts": {
    "build": "lerna run build",
    "lerna:publish": "yarn lerna run build && lerna publish --no-private",
    "republish": "lerna exec -- npm publish",
    "lint": "prettier . --write"
  },
  "type": "module"
}